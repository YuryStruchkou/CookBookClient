{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": [
      "bootstrap",
      "jquery",
      "jqueryui"
    ]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
